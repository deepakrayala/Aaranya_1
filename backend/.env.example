# The port used by the Express server.
PORT=4000

# Set to development, test, or production.
NODE_ENV=development

# Allowed frontend origin for browser requests with credentials.
FRONTEND_ORIGIN=http://localhost:3737

# PostgreSQL connection string. Keep real credentials in a local .env file only.
DATABASE_URL=postgresql://<username>:<password>@localhost:5432/aranya_db
